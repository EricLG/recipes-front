export interface ingredient {
    id: number;
    name: string; // Colonne C
    kiloCalories: number; // Colonne D
    proteins: number; // Colonne E - Protéines
    fats: number; // Colonne F - Lipides
    carbohydrates: number; // Colonne G - Glucides
    totalSugars: number; // Colonne H -Sucres totaux
    fibers: number; // Colonne I - Fibres
}
